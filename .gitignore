# Ignore newly created files by default.
*

# Keep directories visible so Git can reach allowed source files.
!**/

# Keep this configuration file and C++ sources.
!.gitignore
!**/*.cpp

# macOS debug symbols produced by C++ builds.
*.dSYM/
